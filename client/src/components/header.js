import react from "react"

import navbar from './Navbar'

function Header () {
    return(
        <header className="fixed-header">
            <navbar/>
        </header>
    )
}
export default Header